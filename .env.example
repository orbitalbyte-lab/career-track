# Database connection
# Leave unset to use the default local SQLite database.
DATABASE_URL=sqlite:///career_track.db

# JWT signing secret
# Generate a real secret locally. Never commit the real value.
JWT_SECRET_KEY=replace-with-a-secure-random-secret
